# TODO

- [ ] Add `.env.example` with all environment variables needed
- [x] Replace hardcoded OpenRouter API key in `src/api/ai.ts` with `import.meta.env` variables

- [x] Replace hardcoded movie API base + key in `public/meta.php` with values loaded from `.env`

- [x] Replace hardcoded movie API base in `public/api_proxy.php` with values loaded from `.env`

- [x] Replace hardcoded movie API base in `check_cors.js` with env usage

- [x] Replace hardcoded movie API base in `vite.config.ts` with env usage

- [x] Add minimal PHP `.env` loader helper (e.g. `public/env.php`)

- [x] Create `README_GUIDELINE.md` deployment + selling-safe instructions

- [ ] Update `README.md` to reference `.env.example` and `README_GUIDELINE.md`
- [ ] Run `npm run build` / `npm run lint` to verify

